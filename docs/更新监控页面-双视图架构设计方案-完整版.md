# 更新监控页面 - 双视图架构设计方案（完整版）

## 文档说明

本方案基于归档监控页面的成功设计模式，将其优秀的架构理念、数据展示方法和交互逻辑全面迁移应用到更新监控页面的改造中，确保两个监控页面具备一致的设计风格和用户体验。

---

## 一、从归档监控学习的六大设计精髓

### 1.1 清晰的双视图架构

**核心理念**：同一业务数据，支持项目维度和个人维度两种聚合方式

| 维度 | 归档监控 | 迁移到更新监控 |
|-----|---------|--------------|
| **项目视图** | 以项目为单位聚合归档数据 | 以项目为单位聚合更新数据 |
| **个人视图** | 以经办人为单位聚合归档数据 | 以经办人为单位聚合更新数据 |
| **切换保持状态** | 视图切换保留全局筛选条件 | 同样保留全局筛选条件 |

### 1.2 完整的信息层级体系

**四层信息架构**：
```
第一层：全局筛选层（年度、项目）
   ↓ 控制数据范围
第二层：汇总统计层（KPI卡片概览）
   ↓ 提供整体印象
第三层：列表明细层（项目列表/经办人列表）
   ↓ 展示具体数据
第四层：详情展开层（趋势图 + 问题列表）
   ↓ 深入分析问题
```

**设计要点**：
- 每层信息清晰独立，不混杂
- 上层筛选影响下层数据
- 详情展开不影响列表状态

### 1.3 模态框详情页设计模式

**归档监控的模态框结构**：
1. 顶部：标题 + 关闭按钮
2. 第一区域：详细统计面板（卡片式）
3. 第二区域：趋势图（Chart.js折线图）
4. 第三区域：问题记录列表（可折叠分组）

**迁移原则**：
- ✅ 保留三区域结构
- ✅ 采用相同的图表库和配置
- ✅ 保持一致的交互方式（ESC关闭、背景遮罩）
- ➕ 增加热力图展示（更新监控特色）

### 1.4 趋势图数据可视化方法

**归档监控的成功做法**：
- 按时间维度智能分组（全年度→半年，单年度→月）
- 规定周期基准线作为对比参照（虚线）
- 自动跳过无数据时间点
- 鼠标悬停显示详细数据
- 多系列数据用不同颜色区分

**迁移到更新监控**：
- 保持相同的时间分组逻辑
- 规定周期改为"次月月底"基准
- 保持图表配置和交互方式

### 1.5 详情列表的设计方法

**归档监控的列表特征**：
- 按严重程度分级展示（严重/中度/轻微/待处理）
- 可折叠/展开每个分组
- "显示所有记录"复选框（包括正常记录）
- 字段设计完整（业务类型、编号、名称、经办人、日期、天数）

**迁移到更新监控**：
- 采用相同的分级标准
- 保持折叠展开交互
- 增加所属项目字段（个人视图需要）

### 1.6 全局筛选的设计理念

**归档监控的筛选逻辑**：
- 年度筛选：全部年度 / 具体年份
- 项目筛选：全部项目 / 具体项目
- 筛选器位于顶部，影响全页面数据
- 筛选状态清晰展示

**迁移到更新监控**：
- 保持相同的筛选器设计
- ➕ 增加时间范围快捷筛选（本周/本月/本年度）

---

## 二、更新监控页面完整设计方案

### 2.1 核心概念定义

#### 项目视图
- **定义**：基于全局筛选后，展示所有项目的更新监控统计数据
- **数据维度**：以项目为单位
- **全局项目筛选的作用**：
  - 选择具体项目：仅展示该项目的数据和热力图
  - 选择"全部项目"：展示所有项目的数据列表

#### 个人视图
- **定义**：基于全局筛选后，展示所有经办人的更新监控统计数据
- **数据维度**：以每个经办人为单位
- **全局项目筛选的作用**：
  - 选择具体项目：仅展示在该项目中有业务的经办人
  - 选择"全部项目"：展示所有经办人

#### 更新监控的业务范围

**监控对象**：
1. 采购业务更新（公告发布、结果公示）
2. 合同业务更新（签订日期、关键字段）
3. 支付业务更新（支付金额、支付日期）
4. 结算业务更新（结算信息）

**准时规则**：业务发生后**次月月底前**完成录入视为准时

```python
# 准时判断逻辑
业务日期：2024-12-20
截止日期：2025-01-31（次月月底）
录入日期：2025-01-15
结论：准时✓（录入日期 ≤ 截止日期）

# 延迟判断逻辑
业务日期：2024-12-20
截止日期：2025-01-31
录入日期：2025-02-05
结论：延迟5天✗（2月5日 - 1月31日）
```

### 2.2 页面布局设计

#### 项目视图完整布局

```
┌─────────────────────────────────────────────────────────────┐
│ 📊 更新监控                                    [全局筛选▼]  │
│ [项目视图 ✓] [个人视图]                                    │
│ 当前筛选: 2024年 · 全部项目                                │
├─────────────────────────────────────────────────────────────┤
│ 汇总统计卡片（6张 - 参照归档监控）                        │
│ ┌────────┬────────┬────────┬────────┬────────┬────────┐   │
│ │项目总数│采购更新│合同更新│支付更新│综合率  │平均周期│   │
│ │  15个  │ 80/100 │ 45/50  │120/150 │  82%   │ 5.2天  │   │
│ │        │ 80%    │ 90%    │ 80%    │        │        │   │
│ └────────┴────────┴────────┴────────┴────────┴────────┘   │
├─────────────────────────────────────────────────────────────┤
│ 项目列表表格（参照归档监控的表格设计）                    │
│ ┌───────────────────────────────────────────────────────┐  │
│ │项目名称    │采购  │合同  │支付  │结算  │综合率│操作 │  │
│ │PRJ001-A项目│8/10  │9/10  │10/10 │5/10  │██80%│详情 │  │
│ │            │6天90%│4天90%│3天80%│5天75%│      │     │  │
│ │PRJ002-B项目│...   │...   │...   │...   │██85%│详情 │  │
│ └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 【当全局选择了具体项目时显示】                             │
│                                                             │
│ 月度更新热力图（更新监控特色，参照归档监控的趋势图位置）  │
│ ┌───────────────────────────────────────────────────────┐  │
│ │        1月  2月  3月  4月  5月  6月  7月 ...          │  │
│ │ 采购   [✓] [✓] [×] [✓] [✓] [✓] [×] ...          │  │
│ │ 合同   [✓] [×] [✓] [✓] [✓] [✓] [✓] ...          │  │
│ │ 支付   [✓] [✓] [✓] [✓] [×] [✓] [✓] ...          │  │
│ │ 结算   [✓] [✓] [✓] [×] [✓] [✓] [✓] ...          │  │
│ │ ✓准时 ×延迟 ⊙待录入                                   │  │
│ └───────────────────────────────────────────────────────┘  │
│                                                             │
│ 更新准时率趋势图（参照归档监控的Chart.js折线图）          │
│ ┌───────────────────────────────────────────────────────┐  │
│ │  Chart.js 折线图                                       │  │
│ │  100%┤─────────────────────────────              │  │
│ │   80%┤●──●──●──●─────────────            │  │
│ │   60%┤                                               │  │
│ │   40%┤                                               │  │
│ │      └─1月─2月─3月─4月─5月─...                       │  │
│ │  ──● 采购准时率  ──● 合同准时率  ──● 支付准时率      │  │
│ └───────────────────────────────────────────────────────┘  │
│                                                             │
│ 延迟/待更新记录列表（参照归档监控的问题列表设计）          │
│ ▼ 严重延迟（>10天）- 5条                                  │
│   ┌─────────────────────────────────────────────────┐     │
│   │类型│编号 │名称│经办人│业务日期│应录日│延迟天数│     │
│   │采购│P001 │... │张三  │12-20   │01-31 │15天    │     │
│   │合同│C002 
│... │张三  │12-20   │01-31 │15天    │     │
│   │合同│C002 │... │李四  │12-25   │01-31 │12天    │     │
│   └─────────────────────────────────────────────────┘     │
│ ▼ 中度延迟（5-10天）- 8条                                 │
│ ▶ 轻微延迟（1-4天）- 3条                                  │
│ ▶ 待录入（0天）- 12条                                     │
│ □ 显示所有记录（包括已准时录入）                           │
│                                                             │
│ [导出Excel] [导出详细报告]                                 │
└─────────────────────────────────────────────────────────────┘
```

#### 个人视图完整布局

```
┌─────────────────────────────────────────────────────────────┐
│ 📊 更新监控                                    [全局筛选▼]  │
│ [项目视图] [个人视图 ✓]                                    │
│ 当前筛选: 2024年 · 全部项目                                │
├─────────────────────────────────────────────────────────────┤
│ 汇总统计卡片（6张）                                        │
│ ┌────────┬────────┬────────┬────────┬────────┬────────┐   │
│ │经办人数│采购更新│合同更新│支付更新│综合率  │平均周期│   │
│ │  8人   │ 80/100 │ 45/50  │120/150 │  82%   │ 5.2天  │   │
│ └────────┴────────┴────────┴────────┴────────┴────────┘   │
├─────────────────────────────────────────────────────────────┤
│ 经办人列表表格                                              │
│ ┌───────────────────────────────────────────────────────┐  │
│ │姓名│采购 │合同 │支付 │结算 │综合率│项目数│业务量│操作│  │
│ │张三│10/12│8/10 │15/20│5/8  │██85%│3个   │45笔 │详情│  │
│ │    │6天  │4天  │3天  │5天  │      │      │     │    │  │
│ │李四│8/10 │6/8  │12/15│3/5  │██88%│2个   │33笔 │详情│  │
│ └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 【个人视图下始终显示】                                     │
│                                                             │
│ 全员更新准时率趋势图（所有经办人的汇总数据）              │
│ ┌───────────────────────────────────────────────────────┐  │
│ │  Chart.js 折线图 - 全员平均表现                       │  │
│ │  ──● 所有人采购平均  ──● 所有人合同平均              │  │
│ │  ──● 所有人支付平均  ──● 所有人结算平均              │  │
│ └───────────────────────────────────────────────────────┘  │
│                                                             │
│ 全员延迟/待更新记录列表（含经办人筛选功能）                │
│ 经办人快速筛选: [全部▼] [张三] [李四] [王五] ...         │
│ ▼ 严重延迟（>10天）- 15条（涉及多个经办人）               │
│   ┌─────────────────────────────────────────────────┐     │
│   │类型│编号│名称│经办人│项目 │业务日期│应录│延迟│     │
│   │采购│P001│... │张三  │A项目│12-20   │01-31│15天│     │
│   │合同│C002│... │李四  │B项目│12-25   │01-31│12天│     │
│   └─────────────────────────────────────────────────┘     │
│ ▼ 中度延迟（5-10天）- 20条                                 │
│ ▶ 轻微延迟（1-4天）- 10条                                  │
│ ▶ 待录入（0天）- 30条                                      │
│ □ 显示所有记录（包括已准时录入）                           │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 模态框详情页设计

#### 项目详情模态框（参照归档监控的模态框结构）

```
┌─────────────────────────────────────────────────────────────┐
│ 项目详情: PRJ001 - A项目                      [关闭 ×]    │
├─────────────────────────────────────────────────────────────┤
│ 详细统计面板（卡片式，参照归档监控）                      │
│ ┌──────────┬──────────┬──────────┬──────────┐            │
│ │ 采购更新 │ 合同更新 │ 支付更新 │ 结算更新 │            │
│ │  8/10    │  9/10    │  10/10   │  5/10    │            │
│ │  准时率  │  准时率  │  准时率  │  准时率  │            │
│ │  90%     │  90%     │  80%     │  75%     │            │
│ │  平均6天 │  平均4天 │  平均3天 │  平均5天 │            │
│ └──────────┴──────────┴──────────┴──────────┘            │
├─────────────────────────────────────────────────────────────┤
│ 月度更新热力图（更新监控特色）                            │
│ ┌───────────────────────────────────────────────────────┐  │
│ │        1月  2月  3月  4月  5月  6月  7月  8月 ...    │  │
│ │ 采购   [✓] [✓] [×] [✓] [✓] [✓] [×] [✓] ...    │  │
│ │ 合同   [✓] [×] [✓] [✓] [✓] [✓] [✓] [✓] ...    │  │
│ │ 支付   [✓] [✓] [✓] [✓] [×] [✓] [✓] [✓] ...    │  │
│ │ 结算   [✓] [✓] [✓] [×] [✓] [✓] [✓] [✓] ...    │  │
│ └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 更新准时率趋势图（参照归档监控的Chart.js实现）            │
│ ┌───────────────────────────────────────────────────────┐  │
│ │  Chart.js 折线图 + 基准线（虚线）                     │  │
│ │  100%┤─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─                     │  │
│ │   80%┤●──●──×──●──●──●─────              │  │
│ │      └─1月─2月─3月─4月─5月─6月─...                   │  │
│ │  ─ ─ 目标准时率100%  ──● 实际准时率               │  │
│ └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 延迟记录明细（参照归档监控的分组可折叠设计）              │
│ 模块筛选: [全部] [采购] [合同] [支付] [结算]             │
│                                                             │
│ ▼ 严重延迟（>10天）- 5条                                  │
│   ┌─────────────────────────────────────────────────┐     │
│   │类型│编号 │业务日期│录入日期│应录日│延迟│经办人│     │
│   │采购│P001 │12-20   │02-05   │01-31 │15天│张三  │     │
│   │合同│C002 │12-25   │02-08   │01-31 │12天│李四  │     │
│   └─────────────────────────────────────────────────┘     │
│ ▼ 中度延迟（5-10天）- 8条                                 │
│ ▶ 轻微延迟（1-4天）- 3条                                  │
│ ▶ 待录入（未录入）- 12条                                  │
│ □ 显示所有记录（包括已准时录入）                           │
│                                                             │
│ 经办人分布统计                                              │
│ ┌───────────────────────────────────────────────────────┐  │
│ │ 张三: 15笔（采购8、合同5、支付2）                     │  │
│ │ 李四: 10笔（采购3、合同4、支付3）                     │  │
│ │ 王五: 7笔（采购2、合同2、支付2、结算1）               │  │
│ └───────────────────────────────────────────────────────┘  │
│                                                             │
│ [导出项目详情] [打印报表]                                  │
└─────────────────────────────────────────────────────────────┘
```

#### 经办人详情模态框

```
┌─────────────────────────────────────────────────────────────┐
│ 经办人详情: 张三                              [关闭 ×]    │
├─────────────────────────────────────────────────────────────┤
│ 详细统计面板                                                │
│ ┌──────────┬──────────┬──────────┬──────────┐            │
│ │ 采购更新 │ 合同更新 │ 支付更新 │ 结算更新 │            │
│ │  10/12   │  8/10    │  15/20   │  5/8     │            │
│ │  准时率  │  准时率  │  准时率  │  准时率  │            │
│ │  83%     │  80%     │  75%     │  62%     │            │
│ │  平均6天 │  平均4天 │  平均3天 │  平均5天 │            │
│ └──────────┴──────────┴──────────┴──────────┘            │
│                                                             │
│ 负责项目: A项目（15笔）、B项目（18笔）、C项目（12笔）     │
├─────────────────────────────────────────────────────────────┤
│ 该经办人的更新准时率趋势图                                  │
│ ┌───────────────────────────────────────────────────────┐  │
│ │  Chart.js 折线图 - 张三的个人表现趋势                 │  │
│ └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 该经办人的延迟记录明细                                      │
│ 项目筛选: [全部] [A项目] [B项目] [C项目]                  │
│                                                             │
│ ▼ 严重延迟（>10天）- 2条                                  │
│ ▼ 中度延迟（5-10天）- 5条                                 │
│ ▶ 轻微延迟（1-4天）- 3条                                  │
│ ▶ 待录入（未录入）- 8条                                   │
│                                                             │
│ 项目分布统计                                                │
│ ┌───────────────────────────────────────────────────────┐  │
│ │ A项目: 15笔业务（准时率80%）                          │  │
│ │ B项目: 18笔业务（准时率85%）                          │  │
│ │ C项目: 12笔业务（准时率90%）                          │  │
│ └───────────────────────────────────────────────────────┘  │
│                                                             │
│ [导出个人详情] [打印报表]                                  │
└─────────────────────────────────────────────────────────────┘
```

### 2.4 时间范围查询功能（参照归档监控的筛选设计）

#### 快捷查询入口（主页底部）

```
┌─────────────────────────────────────────────────────────────┐
│ 快捷查询 - 最近更新记录                                    │
│ ┌───────────────────────────────────────────────────────┐  │
│ │ 📋 本周新增: 12条    本月新增: 50条    本年度: 600条 │  │
│ │ [查看本周详情] [查看本月详情] [高级查询 >>]          │  │
│ └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### 独立查询页面（/monitoring/update-records/）

```
┌─────────────────────────────────────────────────────────────┐
│ 📋 更新记录查询                          [返回更新监控]    │
├─────────────────────────────────────────────────────────────┤
│ 筛选条件（参照归档监控的全局筛选设计）                    │
│ ┌───────────────────────────────────────────────────────┐  │
│ │ 时间范围: ● 本周  ○ 本月  ○ 本年度  ○ 自定义       │  │
│ │ 自定义:   [2024-12-01] 至 [2024-12-31]  （最多90天）│  │
│ │ 业务模块: ☑ 采购  ☑ 合同  ☑ 支付  ☑ 结算            │  │
│ │ 经办人:   [全部 ▼]  [张三] [李四] ...               │  │
│ │ 项目:     [全部 ▼]  [PRJ001] [PRJ002] ...           │  │
│ │ 关键词:   [搜索业务名称或编号...]                     │  │
│ │ [重置] [查询]                                         │  │
│ └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 统计摘要                                                    │
│ ┌──────┬──────┬──────┬──────┬──────┬──────┐            │
│ │总记录│采购  │合同  │支付  │结算  │准时率│            │
│ │ 120  │ 25   │ 35   │ 45   │ 15   │ 82%  │            │
│ └──────┴──────┴──────┴──────┴──────┴──────┘            │
├─────────────────────────────────────────────────────────────┤
│ 更新记录列表（分页展示，每页50条）                        │
│ ┌───────────────────────────────────────────────────────┐  │
│ │录入日期│模块│编号  │名称    │经办人│业务日期│项目  │  
││
│ │01-05   │采购│P001  │XX采购  │张三  │12-20   │A项目 ││  │
│ │01-05   │合同│C001  │XX合同  │李四  │12-22   │B项目 ││  │
│ │01-06   │支付│PAY01 │XX支付  │张三  │12-25   │A项目 ││  │
│ └───────────────────────────────────────────────────────┘  │
│ [« 上一页] 第1页/共3页 [下一页 »]                         │
├─────────────────────────────────────────────────────────────┤
│ 导出功能                                                    │
│ [导出当前页Excel] [导出全部数据] [导出PDF报告]            │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、数据计算逻辑（参照归档监控）

### 3.1 更新准时率计算

```python
# 准时判断（参照归档监控的归档周期计算方式）
业务日期 = 采购公示日期 / 合同签订日期 / 支付日期 / 结算日期
截止日期 = 业务日期所在月份的次月月底
录入日期 = 数据创建时间

if 录入日期 <= 截止日期:
    状态 = "准时"
else:
    延迟天数 = (录入日期 - 截止日期).days
    状态 = "延迟"

# 准时率计算
准时率 = 准时事件数 / 总事件数 × 100%
```

### 3.2 延迟严重程度分级（参照归档监控的分级方式）

```python
# 参照归档监控的严重/中度/轻微/待处理分级
if not 已录入:
    级别 = "待录入"
elif 延迟天数 > 10:
    级别 = "严重延迟"
elif 延迟天数 >= 5:
    级别 = "中度延迟"
elif 延迟天数 >= 1:
    级别 = "轻微延迟"
else:
    级别 = "准时"
```

### 3.3 趋势图数据分组（完全参照归档监控）

```python
# 与归档监控保持完全一致的时间分组逻辑
if year_filter == 'all':
    # 全年度模式：按半年分组
    groups = ['2023-H1', '2023-H2', '2024-H1', '2024-H2', ...]
    group_by = HALF_YEAR(business_date)
else:
    # 单年度模式：按月分组
    groups = ['1月', '2月', '3月', ..., '12月']
    group_by = MONTH(business_date)

# 计算每组的平均准时率
for group in groups:
    on_time_count = COUNT(WHERE status='准时' AND date IN group)
    total_count = COUNT(WHERE date IN group)
    trend_data[group] = on_time_count / total_count × 100%
```

---

## 四、技术实现方案（参照归档监控的成功实践）

### 4.1 服务层架构（复用归档监控的设计模式）

#### 文件结构
```
project/services/monitors/
├── update_statistics.py          # 更新统计核心服务（新建，参照archive_statistics.py）
├── update_problem_detector.py    # 超期问题检测器（已存在，扩展）
├── update_statistics_facade.py   # 统一门面（新建，参照归档监控模式）
├── utils/
│   ├── time_grouping.py          # 时间分组工具（复用）
│   └── persons.py                # 人员统计工具（复用）
```

#### 核心服务类（参照ArchiveStatisticsService）

```python
class UpdateStatisticsService:
    """
    更新监控统计服务
    设计理念：完全参照ArchiveStatisticsService的成功模式
    """
    
    def get_projects_update_overview(self, year_filter=None, project_filter=None):
        """
        获取项目维度的更新监控概览
        【对应归档监控的get_projects_archive_overview】
        
        Returns:
            dict: {
                'summary': {
                    'project_count': 15,
                    'procurement_stats': {'total': 100, 'updated': 80, 'on_time_rate': 90.0},
                    'contract_stats': {...},
                    'payment_stats': {...},
                    'settlement_stats': {...},
                    'overall_on_time_rate': 82.0
                },
                'projects': [
                    {
                        'project_code': 'PRJ001',
                        'project_name': 'A项目',
                        'procurement_count': 10,
                        'procurement_updated': 8,
                        'procurement_on_time_rate': 90.0,
                        'contract_count': 5,
                        'contract_on_time_rate': 90.0,
                        'payment_count': 15,
                        'payment_on_time_rate': 80.0,
                        'settlement_count': 10,
                        'settlement_on_time_rate': 75.0,
                        'overall_on_time_rate': 85.0
                    },
                    ...
                ]
            }
        """
        pass
    
    def get_persons_update_overview(self, year_filter=None, project_filter=None):
        """
        获取个人维度的更新监控概览
        【对应归档监控的get_persons_archive_overview】
        """
        pass
    
    def get_project_trend_and_problems(self, project_code, year_filter=None):
        """
        获取单个项目的趋势图和延迟记录
        【对应归档监控的get_project_trend_and_problems】
        
        Returns:
            dict: {
                'heatmap_data': {...},           # 热力图数据（更新监控特色）
                'procurement_trend': [...],      # 采购准时率趋势
                'contract_trend': [...],         # 合同准时率趋势
                'payment_trend': [...],          # 支付准时率趋势
                'settlement_trend': [...],       # 结算准时率趋势
                'problems': {
                    'severe': [...],             # 严重延迟记录
                    'moderate': [...],           # 中度延迟记录
                    'minor': [...],              # 轻微延迟记录
                    'pending': [...]             # 待录入记录
                }
            }
        """
        pass
    
    def get_all_persons_trend_and_problems(self, year_filter=None, project_filter=None):
        """
        获取所有经办人的汇总趋势图和延迟记录（用于个人视图主页面）
        【对应归档监控的get_all_persons_trend_and_problems】
        """
        pass
    
    def get_person_trend_and_problems(self, person_name, year_filter=None, project_filter=None):
        """
        获取单个经办人的趋势图和延迟记录（用于查看详情时）
        【对应归档监控的get_person_trend_and_problems】
        """
        pass
    
    def get_time_range_records(self, time_range, filters):
        """
        获取时间范围内的更新记录（独立查询页面使用）
        【更新监控新增功能】
        
        Args:
            time_range: 'week', 'month', 'year', 'custom'
            filters: {
                'start_date': datetime,
                'end_date': datetime,
                'modules': ['procurement', 'contract', ...],
                'person': str,
                'project': str,
                'page': int
            }
        
        Returns:
            dict: {
                'summary': {...},
                'records': [...],
                'pagination': {...}
            }
        """
        pass
```

### 4.2 视图层实现（参照归档监控的视图函数）

```python
# project/views_monitoring.py

def update_monitor(request):
    """
    更新监控主页面
    【完全参照archive_monitor()的实现模式】
    """
    # 1. 解析全局筛选参数（与归档监控一致）
    year_filter = request.GET.get('year', 'all')
    project_filter = request.GET.get('project', None)
    view_mode = request.GET.get('view_mode', 'project')
    target_code = request.GET.get('target_code', None)  # 详情目标
    
    # 2. 初始化统计服务
    stats_service = UpdateStatisticsService()
    
    # 3. 获取概览数据
    if view_mode == 'project':
        overview_data = stats_service.get_projects_update_overview(
            year_filter=year_filter,
            project_filter=project_filter
        )
        # 如果选择了具体项目，获取趋势和问题数据
        trend_and_problems = None
        if project_filter:
            trend_and_problems = stats_service.get_project_trend_and_problems(
                project_code=project_filter,
                year_filter=year_filter
            )
    else:  # person 视图
        overview_data = stats_service.get_persons_update_overview(
            year_filter=year_filter,
            project_filter=project_filter
        )
        # 个人视图下始终显示所有经办人的趋势和问题
        trend_and_problems = stats_service.get_all_persons_trend_and_problems(
            year_filter=year_filter,
            project_filter=project_filter
        )
    
    # 4. 如果有详情目标，准备模态框数据
    detail_data = None
    if target_code:
        if view_mode == 'project':
            detail_data = stats_service.get_project_trend_and_problems(
                project_code=target_code,
                year_filter=year_filter
            )
        else:
            detail_data = stats_service.get_person_trend_and_problems(
                person_name=target_code,
                year_filter=year_filter,
                project_filter=project_filter
            )
    
    # 5. 构建上下文（与归档监控保持一致的数据结构）
    context = {
        'page_title': '更新监控',
        'view_mode': view_mode,
        'year_filter': year_filter,
        'project_filter': project_filter,
        'overview_data': overview_data,
        'trend_and_problems': trend_and_problems,
        'detail_data': detail_data,
        'target_code': target_code,
    }
    
    return render(request, 'monitoring/update_monitor.html', context)


def update_records(request):
    """
    更新记录查询页面（独立页面）
    【更新监控新增功能】
    """
    # 解析查询参数
    time_range = request.GET.get('time_range', 'week')
    filters = {
        'start_date': request.GET.get('start_date'),
        'end_date': request.GET.get('end_date'),
        'modules': request.GET.getlist('modules'),
        'person': request.GET.get('person'),
        'project': request.GET.get('project'),
        'page': int(request.GET.get('page', 1))
    }
    
    # 获取查询结果
    stats_service = UpdateStatisticsService()
    result = stats_service.get_time_range_records(time_range, filters)
    
    context = {
        'page_title': '更新记录查询',
        'time_range': time_range,
        'filters': filters,
        'result': result,
    }
    
    return render(request, 'monitoring/update_records.html', context)
```

### 4.3 前端交互实现（参照归档监控的JavaScript）

```javascript
// 视图切换（与归档监控完全一致）
function switchView(mode) {
    const url = new URL(window.location.href);
    url.searchParams.set('view_mode', mode);
    url.searchParams.delete('target_code');  // 切换视图时清除详情
    window.location.href = url.toString();
}

// 查看详情（与归档监控完全一致）
function viewDetail(targetCode) {
    const url = new URL(window.location.href);
    url.searchParams.set('target_code', targetCode);
    window.location.href = url.toString();
}

// 关闭详情（与归档监控完全一致）
function closeDetail() {
    const url = new URL(window.location.href);
    url.searchParams.delete('target_code');
    window.location.href = url.toString();
}

// 渲染趋势图（参照归档监控的Chart.js配置）
function renderTrendChart(canvasId, trendData) {
    const ctx = document.getElementById(canvasId).getContext('2d');
    new Chart(ctx, {
        type: 'line',
        data: {
            labels: trendData.labels,
            datasets: [
                {
                    label: '采购准时率',
                    data: trendData.procurement,
                    borderColor: '#0d6efd',
                    backgroundColor: 'rgba(13, 110, 253, 0.1)',
                },
                {
                    label: '合同准时率',
                    data: trendData.contract,
                    borderColor: '#198754',
                    backgroundColor: 'rgba(25, 135, 84, 0.1)',
                },
                {
                    label: '支付准时率',
                    data: trendData.payment,
                    borderColor: '#fd7e14',
                    backgroundColor: 'rgba(253, 126, 20, 0.1)',
                },
                {
                    label: '目标准时率100%',
                    data: Array(trendData.labels.length).fill(100),
                    borderColor: '#dc3545',
                    borderDash: [5, 5],
                    fill: false
                }
            ]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            spanGaps: true,  // 自动跳过无数据点
            plugins: {
                tooltip: {
                    mode: 'index',
                    intersect: false
                },
                legend: {
                    position: 'bottom'
                }
            },
            scales: {
                y: {
                    beginAtZero: true,
                    max: 100,
                    ticks: {
                        callback: function(value) {
                            return value + '%';
                        }
                    }
                }
            }
        }
    });
}
```

---

## 五、实施计划（参照归档监控的开发节奏）

### 5.1 迭代式开发（总计11-13个工作日）

#### 阶段0：准备工作（1天）
- [ ] 研读归档监控的代码实现
- [ ] 准备数据库索引和缓存策略
- [ ] 确认时间口径和计算规则

#### 阶段1：核心功能开发（5天）- P0
**Day 1-2: 服务层开发**
- [ ] 
创建UpdateStatisticsService类（参照ArchiveStatisticsService）
- [ ] 实现项目视图和个人视图的概览方法
- [ ] 实现趋势数据计算和问题检测方法
- [ ] 编写单元测试

**Day 3-4: 视图层和模板开发**
- [ ] 修改update_monitor视图函数（参照archive_monitor）
- [ ] 开发主页面模板（参照归档监控的HTML结构）
- [ ] 实现视图切换和详情展示功能
- [ ] 开发模态框详情页

**Day 5: 集成测试和优化**
- [ ] 前后端联调
- [ ] 性能测试和优化
- [ ] 灰度发布P0功能

#### 阶段2：时间查询功能开发（5天）- P0+
**Day 6-7: 时间查询服务**
- [ ] 实现TimeRangeResolver（时间口径解析器）
- [ ] 实现get_time_range_records方法
- [ ] 添加分页和导出功能
- [ ] 权限控制和审计日志

**Day 8-9: 查询页面开发**
- [ ] 开发独立查询页面模板
- [ ] 实现筛选条件和分页
- [ ] 添加快捷入口和面包屑导航
- [ ] 导出功能实现

**Day 10: 验证和发布**
- [ ] 数据准确性验证
- [ ] 性能测试（600+记录场景）
- [ ] 用户培训和文档更新
- [ ] 灰度发布完整功能

#### 阶段3：优化和增强（2天）- P1/P2
**Day 11-12: 体验优化**
- [ ] 热力图渲染优化
- [ ] 缓存命中率监控
- [ ] 移动端适配
- [ ] 用户反馈收集和改进

---

## 六、验收标准（参照归档监控的验收清单）

### 6.1 功能验收

#### 双视图功能
- [ ] **F1**: 项目视图正确展示所有项目及其更新统计
- [ ] **F2**: 个人视图正确展示所有经办人及其更新统计
- [ ] **F3**: 视图切换功能正常，保留全局筛选状态
- [ ] **F4**: 全局筛选（年度、项目）正确影响两个视图的数据范围

#### 详情展示功能
- [ ] **F5**: 点击"查看详情"正确弹出模态框
- [ ] **F6**: 模态框显示完整的统计面板、趋势图、延迟记录
- [ ] **F7**: 热力图正确显示各月的准时/延迟状态
- [ ] **F8**: 趋势图按全年度/单年度正确分组
- [ ] **F9**: 延迟记录按严重程度正确分组
- [ ] **F10**: 可折叠/展开功能正常工作

#### 时间查询功能
- [ ] **F11**: 快捷查询入口显示本周/本月/本年度统计
- [ ] **F12**: 独立查询页面支持多维度筛选
- [ ] **F13**: 查询结果正确分页（每页50条）
- [ ] **F14**: 导出功能正常（Excel/CSV格式）
- [ ] **F15**: 自定义时间范围限制90天生效

### 6.2 数据准确性验收

- [ ] **D1**: 更新准时率计算准确（次月月底判断）
- [ ] **D2**: KPI卡片与详细统计表数据一致
- [ ] **D3**: 热力图状态与实际业务状态一致
- [ ] **D4**: 趋势图数据与数据库查询结果一致
- [ ] **D5**: 延迟天数计算准确
- [ ] **D6**: 时间范围查询结果准确
- [ ] **D7**: 导出数据与页面显示一致

### 6.3 性能验收

| 指标 | 目标值 | 测试方法 | 参照归档监控 |
|------|--------|---------|------------|
| 页面首次加载 | < 2秒 | Chrome DevTools | ✓ 一致 |
| 视图切换响应 | < 1秒 | 实际测试 | ✓ 一致 |
| 详情模态框打开 | < 500ms | 实际测试 | ✓ 一致 |
| 热力图渲染 | < 800ms | 实际测试 | 新增 |
| 趋势图渲染 | < 500ms | 实际测试 | ✓ 一致 |
| 查询页面响应 | < 2秒 | Chrome DevTools | 新增 |
| 大数据量查询 | < 3秒 | 600条记录测试 | 新增 |

### 6.4 用户体验验收

- [ ] **U1**: 界面风格与归档监控完全一致
- [ ] **U2**: 页面布局清晰，信息层次分明
- [ ] **U3**: 交互流程流畅，符合用户习惯
- [ ] **U4**: 热力图可视化直观易懂
- [ ] **U5**: 筛选条件清晰，操作简便
- [ ] **U6**: 错误提示友好，引导明确
- [ ] **U7**: 响应式设计良好，适配不同屏幕
- [ ] **U8**: 加载状态提示清晰

---

## 七、与归档监控的对比总结

### 7.1 完全继承的设计

| 设计元素 | 归档监控 | 更新监控 | 状态 |
|---------|---------|---------|------|
| 双视图架构 | ✓ | ✓ | 完全一致 |
| 四层信息架构 | ✓ | ✓ | 完全一致 |
| KPI卡片设计 | ✓ | ✓ | 完全一致 |
| 列表表格结构 | ✓ | ✓ | 完全一致 |
| 模态框详情页 | ✓ | ✓ | 完全一致 |
| Chart.js趋势图 | ✓ | ✓ | 完全一致 |
| 时间智能分组 | ✓ | ✓ | 完全一致 |
| 问题分级展示 | ✓ | ✓ | 完全一致 |
| 可折叠列表 | ✓ | ✓ | 完全一致 |
| 全局筛选器 | ✓ | ✓ | 完全一致 |
| URL参数管理 | ✓ | ✓ | 完全一致 |

### 7.2 更新监控的特色功能

| 功能 | 说明 | 与归档监控的关系 |
|-----|------|----------------|
| **热力图可视化** | 月度准时/延迟状态热力图 | 新增特色功能 |
| **4个业务模块** | 采购/合同/支付/结算 | 归档监控只有2个 |
| **时间范围查询** | 本周/本月/本年度查询 | 新增独立功能 |
| **独立查询页面** | 专门的记录查询页 | 新增页面 |
| **准时规则不同** | 次月月底前准时 | 归档监控是固定天数 |

### 7.3 设计一致性检查清单

- [x] **布局一致性**：页面结构与归档监控保持一致
- [x] **交互一致性**：视图切换、详情查看方式一致
- [x] **数据层次一致性**：概览→列表→详情的层级关系一致
- [x] **视觉风格一致性**：颜色、字体、间距保持统一
- [x] **代码结构一致性**：服务层、视图层、模板层架构一致
- [x] **命名规范一致性**：变量名、函数名、URL模式一致

---

## 八、技术要点总结

### 8.1 核心设计原则

1. **数据聚合的两个维度**
   - 项目维度：关注项目整体表现
   - 个人维度：关注经办人工作质量

2. **信息展示的四个层次**
   - 全局筛选：控制数据范围
   - 汇总统计：提供整体印象
   - 列表明细：展示具体数据
   - 详情分析：深入问题根源

3. **交互设计的三个关键**
   - 状态保持：切换视图保留筛选条件
   - 渐进展示：从概览到详情逐层深入
   - 清晰反馈：每个操作都有明确的视觉反馈

### 8.2 关键技术点

```python
# 1. 服务层设计模式（参照归档监控）
class UpdateStatisticsService:
    """单一职责：更新监控的统计计算"""
    
class UpdateStatisticsFacade:
    """门面模式：统一入口，隐藏复杂性"""
    
# 2. 数据计算逻辑（与归档监控保持一致）
- 时间分组：全年度按半年、单年度按月
- 准时判断：次月月底前为准时
- 严重程度：>10天严重、5-10天中度、1-4天轻微

# 3. 前端交互模式（完全复用归档监控）
- URL参数控制视图状态
- Chart.js渲染趋势图
- Bootstrap模态框展示详情
```

---

## 九、后续优化方向

### 短期优化（1-2周）
1. **性能优化**
   - 热力图懒加载
   - 查询结果缓存
   - 图表渲染优化

2. **用户体验**
   - 移动端适配
   - 快捷键支持
   - 加载动画优化

### 中期优化（1-2月）
1. **功能增强**
   - 自定义报表
   - 数据对比功能
   - 智能预警

2. **数据分析**
   - 同比环比分析
   - 趋势预测
   - 异常检测

### 长期规划（3-6月）
1. **系统集成**
   - 与归档监控联动分析
   - 跨模块数据关联
   - 统一数据看板

2. **智能化**
   - AI预警系统
   - 自动化报告
   - 个性化推荐

---

## 十、附录

### 附录A：关键代码示例

#### 服务层核心方法（参照归档监控）

```python
def get_projects_update_overview(self, year_filter=None, project_filter=None):
    """
    项目视图概览（完全参照归档监控的实现模式）
    """
    # 1. 构建基础查询（与归档监控一致）
    projects = Project.objects.all()
    if project_filter:
        projects = projects.filter(project_code=project_filter)
    
    # 2. 遍历项目计算统计（与归档监控一致）
    result = []
    for project in projects:
        stats = {
            'project_code': project.project_code,
            'project_name': project.project_name,
            # 计算各模块统计
            'procurement_stats': self._calc_module_stats(
                project, 'procurement', year_filter
            ),
            'contract_stats': self._calc_module_stats(
                project, 'contract', year_filter
            ),
            # ... 其他模块
        }
        result.append(stats)
    
    # 3. 计算汇总统计（与归档监控一致）
    summary = self._calc_summary(result)
    
    return {
        'summary': summary,
        'projects': result
    }
```

### 附录B：URL设计规范

```
# 更新监控主页
/monitoring/update/
  ?view_mode=project          # 
项目视图或person个人视图
  &year=2024                  # 年度筛选
  &project=PRJ001             # 项目筛选
  &target_code=PRJ001         # 详情目标（项目编码或经办人姓名）

# 更新记录查询页（独立页面）
/monitoring/update-records/
  ?time_range=week            # 时间范围：week/month/year/custom
  &start_date=2024-01-01      # 自定义开始日期
  &end_date=2024-01-31        # 自定义结束日期
  &modules=procurement,contract  # 模块筛选
  &person=张三                 # 经办人筛选
  &project=PRJ001             # 项目筛选
  &page=1                     # 分页
```

### 附录C：数据库查询优化建议

```sql
-- 1. 为更新监控添加复合索引（参照归档监控的索引策略）
CREATE INDEX idx_procurement_update ON procurement (
    business_date, created_at, responsible_person, project_id
);

CREATE INDEX idx_contract_update ON contract (
    contract_date, created_at, responsible_person, project_id
);

CREATE INDEX idx_payment_update ON payment (
    payment_date, created_at, responsible_person, project_id
);

CREATE INDEX idx_settlement_update ON settlement (
    completion_date, created_at, responsible_person, project_id
);

-- 2. 查询性能监控
-- 确保主查询执行时间 < 100ms
EXPLAIN ANALYZE SELECT ...;
```

### 附录D：前端组件清单

| 组件 | 文件位置 | 作用 | 参照归档监控 |
|-----|---------|------|------------|
| 视图切换按钮 | update_monitor.html | 切换项目/个人视图 | ✓ |
| KPI卡片 | update_monitor.html | 显示汇总统计 | ✓ |
| 列表表格 | update_monitor.html | 显示项目/经办人列表 | ✓ |
| 热力图 | update_monitor.html | 显示月度状态 | 新增 |
| 趋势图 | trend-chart.js | Chart.js折线图 | ✓ |
| 模态框 | custom-dialog.css | 详情展示 | ✓ |
| 筛选器 | global-filter-component.js | 全局筛选 | ✓ |

---

## 十一、总结与建议

### 11.1 设计总结

本方案成功将归档监控页面的六大设计精髓全面迁移到更新监控页面：

1. ✅ **清晰的双视图架构** - 完全继承
2. ✅ **完整的信息层级体系** - 完全继承
3. ✅ **模态框详情页设计模式** - 完全继承并增强
4. ✅ **趋势图数据可视化方法** - 完全继承
5. ✅ **详情列表的设计方法** - 完全继承
6. ✅ **全局筛选的设计理念** - 完全继承并扩展

### 11.2 创新点

在保持设计一致性的基础上，更新监控增加了以下特色功能：

1. **热力图可视化** - 直观展示月度准时/延迟状态
2. **时间范围查询** - 支持本周/本月/本年度快速查询
3. **独立查询页面** - 专业的数据查询和导出功能
4. **4个业务模块** - 更全面的业务覆盖

### 11.3 实施建议

#### 优先级推荐
1. **P0（第一阶段，5天）**：双视图模式、详情展示、热力图
2. **P0+（第二阶段，5天）**：时间范围查询、独立查询页面
3. **P1（第三阶段，2天）**：性能优化、用户体验增强

#### 成功关键因素
1. **严格参照归档监控** - 复用成功的代码模式和设计理念
2. **保持架构一致性** - 服务层、视图层、模板层结构一致
3. **充分的测试** - 功能测试、性能测试、用户体验测试
4. **迭代式开发** - 分阶段交付，及时收集反馈

#### 风险控制
1. **性能风险** - 热力图渲染优化、查询分页控制
2. **数据准确性** - 准时率计算逻辑的严格测试
3. **用户适应** - 提供清晰的使用指南和培训

### 11.4 预期效果

通过本次改造，更新监控页面将实现：

✅ **数据精准性** - 精准调取用户需要的数据，计算准确
✅ **页面简洁性** - 信息展示清晰，不冗杂，层次分明
✅ **设计一致性** - 与归档监控保持完全一致的设计风格
✅ **功能完整性** - 双视图、详情分析、时间查询功能齐全
✅ **用户满意度** - 满足用户"能够调取到想要的数据"的核心诉求

---

## 文档版本信息

| 项目 | 信息 |
|-----|------|
| **文档版本** | v1.0 完整版 |
| **创建日期** | 2025-11-13 |
| **作者** | Kilo Code |
| **参考文档** | 归档监控页面完整需求文档 |
| **关联文档** | 更新监控页面改造需求方案 |
| **文档状态** | ✅ 待评审 |

---

## 下一步行动

1. **召开需求评审会**
   - 确认设计方案的可行性
   - 与业务方对齐时间口径和准时规则
   - 评估开发工作量和资源需求

2. **技术准备**
   - 搭建开发环境
   - 准备测试数据
   - 配置缓存和索引

3. **开始开发**
   - 按照迭代计划逐步实施
   - 定期同步进度和问题
   - 及时收集反馈并调整

4. **文档维护**
   - 根据评审意见更新文档
   - 补充技术实现细节
   - 更新验收标准

---

**文档结束**

*本文档详细阐述了如何将归档监控页面的成功设计模式全面迁移到更新监控页面，确保两个监控页面具备一致的设计风格、清晰的信息层次和良好的用户体验。*

---

## 快速参考

### 核心对比表

| 对比维度 | 归档监控 | 更新监控 | 一致性 |
|---------|---------|---------|--------|
| 双视图架构 | ✓ 项目视图+个人视图 | ✓ 项目视图+个人视图 | 100% |
| 信息层级 | ✓ 筛选→概览→列表→详情 | ✓ 筛选→概览→列表→详情 | 100% |
| 模态框设计 | ✓ 统计+趋势+问题 | ✓ 统计+热力图+趋势+问题 | 增强 |
| 趋势图 | ✓ Chart.js折线图 | ✓ Chart.js折线图 | 100% |
| 时间分组 | ✓ 半年/月 | ✓ 半年/月 | 100% |
| 问题分级 | ✓ 严重/中度/轻微 | ✓ 严重/中度/轻微 | 100% |
| 业务模块 | 2个（采购、合同） | 4个（+支付、结算） | 扩展 |
| 特色功能 | 归档周期趋势 | 热力图+时间查询 | 差异化 |

### 关键指标

| 指标类型 | 目标值 | 对标归档监控 |
|---------|--------|------------|
| 页面加载 | < 2秒 | ✓ 一致 |
| 视图切换 | < 1秒 | ✓ 一致 |
| 详情展开 | < 500ms | ✓ 一致 |
| 查询响应 | < 2秒 | 新增 |
| 代码复用率 | > 70% | 高度复用 |
| 设计一致性 | > 95% | 高度一致 |

---

*最后更新：2025-11-13*